import Box from "../Box";
import { ProductCardProps } from "./types";

const ProductCard = ({
    product,
    size
}: ProductCardProps) => {
    return (
        <Box>

        </Box>
    )
}

export default ProductCard;